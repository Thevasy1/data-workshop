# 数据工坊 (Data Workshop)

AI 一体化平台中负责数据接入、数据集构建与预处理的模块。作为平台四大模块之一，聚焦于从原始数据到可用训练数据的加工链路。

## Language

**预处理任务 (Preprocess Task)**:
对数据集执行一个或多个处理步骤的任务单元。每个任务可按顺序执行多种处理方式，执行后生成数据集的新版本。
_Avoid_: 数据处理任务、预处理作业

**处理方式 (Process Type)**:
预处理任务的分类标识，定义了任务对数据集执行何种变换。共四种类型，支持多选，按选中顺序依次执行。
_Avoid_: 处理类型、操作类型

- **数据清洗 (Clean)**: 处理空值、过滤异常值、修正格式不一致等脏数据修复操作。
- **数据去重 (Dedup)**: 基于指定字段识别并移除重复记录，支持保留首条或末条策略。
- **标准化 (Normalize)**: 将数值字段按统计方法（Z-score、Min-Max 等）缩放到统一尺度。
- **格式转换 (Format)**: 将数据集从当前格式转换为目标格式（CSV、JSON、Excel 等）。

**数据集版本 (Dataset Version)**:
预处理任务的输出产物。每次预处理执行产生一个不可变的新版本，支持版本间对比和回滚。原始数据集不受影响。
_Avoid_: 覆盖、原地修改

**源数据集 (Source Dataset)**:
经过采集阶段完成、状态为"采集成功"的数据集，可作为预处理任务的输入。预处理任务不直接操作原始数据源。
_Avoid_: 原始数据、上游数据
